cvertex
spike
bare
*.o
.DS_Store

# Scratch input for the asset pipeline, and anything generated from it.
# Nothing here is part of the engine. Never tracked.
assets/local/
src/local/
cvertex.app
cvertex.exe
gen/
cvertex.pdb

# The box editor's default export target (games/editor.c). A generated mesh, not engine.
editor_export.h

# The incubator's own paperwork. It lives in cvertex-lab and must never land here —
# this line is the guard, so a careless copy cannot be committed by accident.
lab/
